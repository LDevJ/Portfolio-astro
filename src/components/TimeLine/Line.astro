---
interface Props{
    fecha: string,
    titulo: string,
    trabajo?:string,
    descripcion:string[]
}

const {fecha, titulo, trabajo, descripcion} = Astro.props;
---

<li class="mb-10 ms-4">
    <div class="absolute w-3 h-3 bg-black rounded-full mt-1.5 -start-1.5 border border-white dark:border-gray-900 dark:bg-gray-700"></div>
    <time class="mb-1 text-sm font-normal leading-none text-black dark:text-gray-500">{fecha}</time>
    <h3 class="text-lg font-semibold text-black dark:text-white uppercase">{titulo}</h3>
    <p class="mb-4 mt-1 text-base font-bold text-dark dark:text-gray-400">{trabajo}</p>
    {descripcion.map((des)=>
    <p class="mb-4 mt-1 text-base font-normal text-dark dark:text-gray-400">{des}</p>)}
</svg></a>
</li>